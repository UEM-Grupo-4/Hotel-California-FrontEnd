## 🚀 Requisitos

Para levantar el frontend **solo es necesario**:

- **Docker Desktop**
  - Disponible para:
    - macOS
    - Windows
    - Linux
- Docker Compose (incluido en Docker Desktop)

👉 No es necesario:
- Node.js
- npm / yarn
- Configuración manual de dependencias

---

## ▶️ Cómo levantar el proyecto

Desde la raíz del repositorio del frontend, ejecutar:

```bash
docker compose up --build
```
